[tool.poetry]
name = "bt-toolkit"
version = "0.1.0"
description = "quoteapi tradeapi toolkit"
authors = ["hengxin.liu <hengxin.liu@inceptio.ai>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.9"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
